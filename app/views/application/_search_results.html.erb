<% items.each do |key, value| %>
  <div>
    <a data-action="search-results#select" href="#">
      <%= hidden_field_tag :item_id, key  %>
      <%= highlight(value, params[:q]) %>
    </a>
  </div>
<% end %>
